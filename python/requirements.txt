# Core dependencies
pytest>=9.0.0
pytest-cov>=4.1.0
python-dotenv>=1.0.0

# Web Framework (Flask)
Flask>=3.0.0
Flask-CORS>=4.0.0
Werkzeug>=3.0.0

# Google Calendar API (optional - ถ้าใช้ mode="api")
google-auth>=2.0.0
google-auth-oauthlib>=1.0.0
google-auth-httplib2>=0.2.0
google-api-python-client>=2.0.0

# Database (optional - สำหรับ production)
SQLAlchemy>=2.0.0
psycopg2-binary>=2.9.0

# Utilities
requests>=2.28.0
pydantic>=2.0.0

# MCP Client (optional - ถ้าใช้ mode="mcp")
# mcp-client>=1.0.0  # uncomment when available
